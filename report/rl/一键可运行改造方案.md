# 一键可运行改造方案

> **日期**: 2026-06-02  
> **目标**: 从当前的"代码链路连通但需手动引导" → "一键启动端到端训练"  
> **工作量估算**: 2-3 天（含脚本 + 2个Java代码修改）

---

## 一、改造总览

### 当前问题

| # | 问题 | 影响 | 修改量 |
|---|------|------|:-----:|
| 1 | `inference_server.py --model` required，没有初始模型无法启动 | 鸡生蛋死锁 | 1 个 Python 文件 |
| 2 | MCTSAgent 未使用 policy 先验（PUCT） | 搜索效率低 | 1 个 Java 文件 |
| 3 | `start_training.bat` 未启动 `train_server.py` | 缺少训练服务 | 1 个脚本 |
| 4 | 没有一键生成初始模型的入口 | 用户不知道第一步做什么 | 1 个脚本 |
| 5 | 3 个进程需要独立窗口管理 | 操作繁琐 | 1 个脚本 |

### 改造后

```
一键运行流程:
  ┌──────────────────────────────────────────────┐
  │  python run_train.py                          │
  │  (或双击 run_train.bat)                       │
  ├──────────────────────────────────────────────┤
  │  Step 1: 检查 Python/Java/Maven 环境           │
  │  Step 2: mvn compile (Java编译)               │
  │  Step 3: 生成初始模型 (随机权重 .pt)            │
  │  Step 4: 启动 inference_server (端口 50051)    │
  │  Step 5: 启动 train_server (端口 50052)        │
  │  Step 6: 启动 Java UCCServer (训练模式)        │
  │  Step 7: 等待训练完成, 清理进程                 │
  └──────────────────────────────────────────────┘
```

---

## 二、具体改造项

### 2.1 `inference_server.py` — 允许无初始模型启动 🔧

**修改**: [`inference_server.py:33`](ucc-ai/python/inference_server.py:33)

当前代码：
```python
def __init__(self, model_path: str, device: str = "cuda"):
    checkpoint = torch.load(model_path, ...)  # model_path required
```

目标：允许 `model_path=None` 时使用随机权重初始化。

```python
def __init__(self, model_path: Optional[str] = None, device: str = "cuda"):
    self.model = MiniResNet(...)
    if model_path and os.path.exists(model_path):
        checkpoint = torch.load(model_path, ...)
        self.model.load_state_dict(checkpoint["model_state_dict"])
    else:
        logger.info("No checkpoint provided, using random weights")
    self.model.eval()
```

同时修改 `main()` 中的 `--model` 从 `required=True` → `required=False, default=None`。

| 修改 | 位置 | 行数 |
|------|------|:----:|
| `__init__` 允许 None | `inference_server.py` | ~5行 |
| `serve()` 参数可选 | `inference_server.py` | ~2行 |
| `main()` 去掉 required | `inference_server.py` | ~1行 |

### 2.2 MCTSAgent — PUCT 策略先验引导搜索 🔧

**修改**: [`MCTSAgent.java`](ucc-ai/src/main/java/io/github/samera2022/chinese_chess/ai/MCTSAgent.java)

#### 2.2.1 扩展阶段存储 policy 先验 (约 +3 行)

在 [`MCTSAgent.java:122-123`](ucc-ai/src/main/java/io/github/samera2022/chinese_chess/ai/MCTSAgent.java:122) 处，创建子节点时，从 nnResult 中获取对应着法的 policy 概率：

```java
// 当前：
MCTSNode child = new MCTSNode(unexpanded, node);
node.children.add(child);

// 改为：从 nnResult 中取 policy 先验
float childPrior = 0.0f;
if (nnResult != null && nnResult.length > 1) {
    // nnResult[0]=value, nnResult[1...]=policy (按棋盘格子排列)
    int policyIdx = unexpanded.getToRow() * cols + unexpanded.getToCol();
    if (policyIdx >= 0 && policyIdx < nnResult.length - 1) {
        childPrior = nnResult[1 + policyIdx];
    }
}
MCTSNode child = new MCTSNode(unexpanded, node, childPrior);
```

#### 2.2.2 构造函数接受 prior (约 +3 行)

```java
MCTSNode(Move move, MCTSNode parent, double prior) {
    this(move, parent);
    this.priorPolicy = prior;
}
```

#### 2.2.3 UCB 改为 PUCT (约 +3 行)

在 [`MCTSAgent.java:300-303`](ucc-ai/src/main/java/io/github/samera2022/chinese_chess/ai/MCTSAgent.java:300) 处：

```java
// 当前（标准UCB1）：
double exploration = EXPLORATION_CONSTANT
    * Math.sqrt(Math.log(parent.visitCount) / child.visitCount);

// 改为（PUCT，使用 priorPolicy）：
double exploration = EXPLORATION_CONSTANT
    * child.priorPolicy
    * Math.sqrt(parent.visitCount) / (1.0 + child.visitCount);
```

**总修改**: 约 10 行 Java 代码。

### 2.3 新增 `scripts/init_model.py` — 初始模型生成脚本 🆕

新建文件: `ucc-ai/python/init_model.py`

```python
"""生成随机权重的初始模型，解决鸡生蛋问题"""
import torch, os, sys
sys.path.insert(0, os.path.dirname(__file__))
from model import MiniResNet

def main():
    out_dir = sys.argv[1] if len(sys.argv) > 1 else "checkpoints"
    os.makedirs(out_dir, exist_ok=True)
    
    rows = 18  # EXPANDED_ROWS (同时支持10×9和18×9推理)
    cols = 9
    model = MiniResNet(14, rows, cols, rule_dim=28, num_res_blocks=5, filters=128)
    
    path = os.path.join(out_dir, "model_init.pt")
    torch.save({
        "model_state_dict": model.state_dict(),
        "iteration": 0,
        "config": {"rows": rows, "cols": cols, "num_res_blocks": 5, "filters": 128}
    }, path)
    print(f"✅ 初始模型已生成: {path}")
    print(f"   参数量: {sum(p.numel() for p in model.parameters()):,}")

if __name__ == "__main__":
    main()
```

### 2.4 新增 `scripts/run_train.sh` — 一键启动脚本 (Linux) 🆕

新建文件: `scripts/run_train.sh`

```bash
#!/bin/bash
set -e

PROJECT_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
PYTHON_DIR="$PROJECT_ROOT/ucc-ai/python"
CHECKPOINT_DIR="$PYTHON_DIR/checkpoints"

echo "============================================================"
echo "  UCC 一键训练启动"
echo "============================================================"

# Step 1: 生成初始模型（如果不存在）
INIT_MODEL="$CHECKPOINT_DIR/model_init.pt"
if [ ! -f "$INIT_MODEL" ]; then
    echo "[1/5] 生成初始模型..."
    cd "$PYTHON_DIR" && python init_model.py "$CHECKPOINT_DIR"
else
    echo "[1/5] 初始模型已存在，跳过"
fi

# Step 2: 编译 Java
echo "[2/5] 编译 Java 模块..."
cd "$PROJECT_ROOT" && mvn compile -pl ucc-core,ucc-common,ucc-ai,ucc-server -am -q
echo "      编译完成"

# Step 3: 启动推理服务 (后台)
echo "[3/5] 启动推理服务 (端口 50051)..."
cd "$PYTHON_DIR" && python inference_server.py \
    --model "$INIT_MODEL" \
    --port 50051 \
    --workers 4 &
INFERENCE_PID=$!
echo "      推理服务 PID: $INFERENCE_PID"
sleep 3

# Step 4: 启动训练服务 (后台)
echo "[4/5] 启动训练服务 (端口 50052)..."
cd "$PYTHON_DIR" && python train_server.py \
    --port 50052 \
    --checkpoint "$INIT_MODEL" \
    --replay-capacity 200000 &
TRAIN_PID=$!
echo "      训练服务 PID: $TRAIN_PID"
sleep 2

# Step 5: 启动 Java 训练
echo "[5/5] 启动 Java 训练引擎..."
cd "$PROJECT_ROOT" && mvn exec:java -pl ucc-server \
    -Dexec.mainClass="io.github.samera2022.chinese_chess.server.UCCServer" \
    -Dexec.args="--train"

# 清理
echo "训练完成，清理子进程..."
kill $TRAIN_PID 2>/dev/null || true
kill $INFERENCE_PID 2>/dev/null || true
echo "完成"
```

### 2.5 新增 `scripts/run_train.bat` — 一键启动脚本 (Windows) 🆕

新建文件: `scripts/run_train.bat`

```batch
@echo off
setlocal enabledelayedexpansion

set "PROJECT_ROOT=%~dp0.."
set "PYTHON_DIR=%PROJECT_ROOT%\ucc-ai\python"
set "CHECKPOINT_DIR=%PYTHON_DIR%\checkpoints"
set "INIT_MODEL=%CHECKPOINT_DIR%\model_init.pt"

echo ============================================================
echo   UCC 一键训练启动 (Windows)
echo ============================================================

:: 寻找 Python
set "PYTHON=%PROJECT_ROOT%\.venv-cpu\Scripts\python.exe"
if not exist "!PYTHON!" set "PYTHON=python"

:: Step 1: 生成初始模型
echo [1/5] 检查初始模型...
if not exist "!INIT_MODEL!" (
    echo       生成初始模型...
    cd /d "!PYTHON_DIR!"
    !PYTHON! init_model.py "!CHECKPOINT_DIR!"
) else (
    echo       初始模型已存在: !INIT_MODEL!
)

:: Step 2: 编译 Java
echo [2/5] 编译 Java 模块...
cd /d "!PROJECT_ROOT!"
call mvn compile -pl ucc-core,ucc-common,ucc-ai,ucc-server -am -q
if !ERRORLEVEL! neq 0 (
    echo [错误] 编译失败
    pause
    exit /b 1
)
echo       编译完成

:: Step 3: 启动推理服务
echo [3/5] 启动推理服务 (端口 50051)...
start "UCC-Inference" cmd /c "!PYTHON! \"!PYTHON_DIR!\inference_server.py" --model "!INIT_MODEL!" --port 50051 --workers 4"
timeout /t 3 /nobreak >nul

:: Step 4: 启动训练服务
echo [4/5] 启动训练服务 (端口 50052)...
start "UCC-Training" cmd /c "!PYTHON! \"!PYTHON_DIR!\train_server.py" --port 50052 --checkpoint "!INIT_MODEL!" --replay-capacity 200000"
timeout /t 2 /nobreak >nul

:: Step 5: 启动 Java 训练引擎
echo [5/5] 启动 Java 训练引擎...
cd /d "!PROJECT_ROOT!"
call mvn exec:java -pl ucc-server -Dexec.mainClass="io.github.samera2022.chinese_chess.server.UCCServer" -Dexec.args="--train"

:: 清理
echo 训练完成，清理进程...
taskkill /fi "WINDOWTITLE eq UCC-Inference" /f >nul 2>&1
taskkill /fi "WINDOWTITLE eq UCC-Training" /f >nul 2>&1
echo 完成
pause
```

### 2.6 更新 `start_training.bat` — 改写为新的一键启动 🆕

用上面的 `run_train.bat` 替代现有的 `start_training.bat`。现有版本缺少 `train_server.py` 启动步骤。

### 2.7 可选的快捷入口 — `run_train.py` (跨平台) 🆕

新增 `ucc-ai/python/run_train.py` 作为跨平台的 Python 入口点：

```python
#!/usr/bin/env python3
"""
一键启动训练 — 跨平台入口点
启动: inference_server + train_server + Java UCCServer
"""
import subprocess, sys, os, time, signal, atexit

PROJECT_ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
PYTHON_DIR = os.path.join(PROJECT_ROOT, "ucc-ai", "python")
CHECKPOINT_DIR = os.path.join(PYTHON_DIR, "checkpoints")

processes = []

def cleanup():
    for p in processes:
        p.terminate()
    print("\n进程已清理")

def main():
    os.makedirs(CHECKPOINT_DIR, exist_ok=True)
    init_model = os.path.join(CHECKPOINT_DIR, "model_init.pt")

    # 1. 生成初始模型
    if not os.path.exists(init_model):
        print("[1/5] 生成初始模型...")
        subprocess.run([sys.executable, "init_model.py", CHECKPOINT_DIR], cwd=PYTHON_DIR, check=True)
    else:
        print("[1/5] 初始模型已存在")

    # 2. Maven 编译
    print("[2/5] 编译 Java...")
    subprocess.run(["mvn", "compile", "-pl", "ucc-core,ucc-common,ucc-ai,ucc-server", "-am", "-q"],
                   cwd=PROJECT_ROOT, check=True)

    # 3. 启动推理服务
    print("[3/5] 启动推理服务 (50051)...")
    p = subprocess.Popen([sys.executable, "inference_server.py",
                          "--model", init_model, "--port", "50051", "--workers", "4"],
                         cwd=PYTHON_DIR)
    processes.append(p)
    time.sleep(3)

    # 4. 启动训练服务
    print("[4/5] 启动训练服务 (50052)...")
    p = subprocess.Popen([sys.executable, "train_server.py",
                          "--port", "50052", "--checkpoint", init_model],
                         cwd=PYTHON_DIR)
    processes.append(p)
    time.sleep(2)

    # 5. 启动 Java 训练
    print("[5/5] 启动 Java 训练引擎...")
    subprocess.run(["mvn", "exec:java", "-pl", "ucc-server",
                    "-Dexec.mainClass=io.github.samera2022.chinese_chess.server.UCCServer",
                    "-Dexec.args=--train"],
                   cwd=PROJECT_ROOT)

if __name__ == "__main__":
    atexit.register(cleanup)
    main()
```

---

## 三、修改汇总

### 3.1 文件清单

| 文件 | 操作 | 修改量 | 优先级 |
|------|:----:|:------:|:------:|
| [`MCTSAgent.java`](ucc-ai/src/main/java/io/github/samera2022/chinese_chess/ai/MCTSAgent.java) | 🔧 修改 | +10行 | **P0** 影响训练效率 |
| [`inference_server.py`](ucc-ai/python/inference_server.py) | 🔧 修改 | +8行 | **P0** 阻塞启动 |
| [`init_model.py`](ucc-ai/python/init_model.py) | 🆕 新建 | +25行 | **P0** 解决鸡生蛋 |
| [`run_train.bat`](scripts/run_train.bat) | 🆕 新建 | +70行 | **P0** 一键启动 |
| [`run_train.sh`](scripts/run_train.sh) | 🆕 新建 | +70行 | **P0** Linux支持 |
| [`run_train.py`](ucc-ai/python/run_train.py) | 🆕 新建 | +70行 | P1 跨平台 |
| [`start_training.bat`](scripts/start_training.bat) | ♻️ 替换 | 用 run_train.bat | P1 |

### 3.2 工作量估算

| 任务 | 时间 | 说明 |
|------|:----:|------|
| MCTSAgent PUCT 改造 | ~1小时 | 10行 Java，需理解 MCTS 逻辑 |
| inference_server 可选 model | ~20分钟 | 8行 Python |
| init_model.py | ~20分钟 | 新建文件 |
| run_train.bat | ~1小时 | 批处理脚本调试 |
| 测试验证 | ~1小时 | 确保3进程正常协作 |
| **合计** | **~3-4小时** | |

### 3.3 执行顺序

```
Week 1:
  Day 1: MCTSAgent PUCT ← 核心算法改进
         inference_server 可选model ← 阻塞项
         init_model.py ← 阻塞项
  Day 2: run_train.bat / run_train.sh
         测试: 全流程跑通
  Day 3-4: 观察训练是否正常收敛
```

---

## 四、验证方法

### 4.1 运行后检查点

```bash
# 检查所有进程是否在运行
# Windows:
tasklist | findstr python
tasklist | findstr java

# Linux:
ps aux | grep -E 'inference_server|train_server|java'

# 检查 gRPC 端口是否监听
netstat -an | findstr 50051
netstat -an | findstr 50052
```

### 4.2 正常训练的日志标志

| 组件 | 正常标志 |
|------|---------|
| `inference_server` | `推理服务已启动，端口: 50051` |
| `train_server` | `训练 gRPC 服务已启动，端口: 50052` |
| `TrainingOrchestrator` | `Iteration 1/2000, Progress: 0.0%, Stage: BASIC` |
| `SelfPlayWorker` | `finished game: ... moves, ... samples collected` |
| `TrainingServiceClient` | `Pushed ... samples to Python training service` |
| `InferenceServicer.UpdateModel` | `模型热更新完成` |

### 4.3 常见故障排查

| 现象 | 原因 | 解决 |
|------|------|------|
| gRPC 连接失败 | 推理/训练服务未启动 | `netstat` 检查端口 |
| 训练无进度 | 样本数 < 32 未触发推送 | 增加 `games_per_iteration` |
| UpdateModel 失败 | 权重格式不匹配 | 检查 MiniResNet 参数一致 |
| Java 编译失败 | protobuf 未生成 | `mvn compile` 单独执行 |

---

## 五、未涵盖的扩展项（后续迭代）

以下不在本方案范围内，但值得后续考虑：

| 事项 | 原因 |
|------|------|
| Windows Docker Desktop 支持 | 降低环境依赖 |
| 训练进度 Web UI | 替代命令行日志 |
| 模型版本管理 | 多个 checkpoint 的组织 |
| 分布式训练 | 多机多卡支持 |
